# Local-only files (server.js is the Vercel server entrypoint, so it ships)
start.cmd
_downloads/
_t_locked.pdf

# Tooling / metadata
node_modules/
.vercel/
*.log

# Secrets travel via Vercel environment variables, never in the bundle
.env
